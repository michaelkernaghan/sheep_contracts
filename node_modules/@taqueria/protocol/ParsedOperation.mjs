// ParsedOperation.ts
import createType from "@taqueria/protocol/Base";
import * as Operation from "@taqueria/protocol/Operation";
var internalSchema2 = Operation.internalSchema.describe("ParsedOperation").omit({ handler: true });
var rawSchema2 = Operation.rawSchema.omit({ handler: true }).describe("ParsedOperation");
var { schemas: generatedSchemas, factory } = createType({
  rawSchema: rawSchema2,
  internalSchema: internalSchema2,
  parseErrMsg: (value) => `Could not parse the following operation: ${value}`,
  unknownErrMsg: "Something went wrong trying to parse an operation"
});
var { create, make, of } = factory;
var schemas = {
  ...generatedSchemas,
  schema: generatedSchemas.schema.transform((val) => val)
};
export {
  create,
  factory,
  generatedSchemas,
  make,
  of,
  rawSchema2 as rawSchema,
  schemas
};
//# sourceMappingURL=ParsedOperation.mjs.map